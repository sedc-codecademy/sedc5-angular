import { Component, Input, OnInit } from "@angular/core";
import { ActivatedRoute } from "@angular/router";

@Component({
    selector: "app-invalid-operation",
    template: `<div>
    INVALID OPERATION
</div>`
})
export class InvalidOperationComponent {
}
